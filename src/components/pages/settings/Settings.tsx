import scss from "./Settings.module.scss";

const Settings = () => {
  return (
    <section className={scss.Settings}>
      <div className="container">
        <div className={scss.content}>Settings</div>
      </div>
    </section>
  );
};

export default Settings;
